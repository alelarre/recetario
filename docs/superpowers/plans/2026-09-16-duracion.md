# La duración — plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `tiempo` pasa a ser uno de cinco valores con relojito, se carga con botones en el editor, se ve en tarjeta, receta y búsqueda, se filtra en categoría y lista por tag, y se ordena en categoría, lista por tag y búsqueda.

**Architecture:** Los valores y su validación viven en `src/recipe.ts` (el esquema del `.md`) y `src/catalogo.ts` los reexporta junto con las funciones puras de contar, filtrar y ordenar. Los íconos son constantes de `src/ui/iconos.ts`. Las pantallas reciben el orden y los filtros como opciones; `src/main.ts` guarda el estado —como `tagsActivos`— y lo limpia al cambiar de pantalla.

**Tech Stack:** TypeScript estricto + Vite, sin framework, Vitest.

**Spec:** `docs/superpowers/specs/2026-09-16-duracion-design.md`

## Global Constraints

- Todo en español rioplatense: UI, comentarios, tests, mensajes de commit.
- Los cinco valores, escritos exactamente así y en este orden: `~15 min`, `~30 min`, `~60 min`, `>60 min`, `>1 día`.
- Cualquier otro texto en `tiempo` se trata como sin duración: no se muestra, no filtra, no ordena.
- El frontmatter sigue con siete claves; no sube `SCHEMA_VERSION`.
- Orden A–Z: igual que hoy (favoritas primero, alfabético). Orden por duración: de `~15 min` a `>1 día`, favoritas mezcladas, alfabético dentro de cada valor, sin duración al final en alfabético.
- Filtro: cada valor encendido suma; se combina con los tags (tiene los tags **y** alguna duración encendida). Sólo en categoría y lista por tag.
- Orden y filtro se pierden al cambiar de pantalla. Las filas no se dibujan si ninguna receta de la lista tiene duración.
- Todo texto de un `.md` pasa por `escapar()`.
- Gates: `npx vitest run`, `npm run typecheck`, `npm run build`.
- Commits locales; nada de push.

---

## Mapa de archivos

| Archivo | Responsabilidad |
|---|---|
| `src/recipe.ts` | `DURACIONES`, `Duracion`, `duracionValida`; `contextoDe` filtra el tiempo inválido |
| `src/catalogo.ts` | Reexporta; `entradaDesdeFila` valida; `ordenarRecetas(entradas, orden)`; `contarDuraciones`; `filtrarPorDuracion`; tipo `Orden` |
| `src/ui/iconos.ts` | `ICONO_DE_DURACION` |
| `src/ui/editor.ts` | Campo «Duración» y `recetaDesdeFormulario` |
| `src/ui/componentes.ts` | Duración en la línea de contexto de `tarjeta`; `filaDuraciones`; `conmutadorOrden` |
| `src/ui/fichas-receta.ts` | Duración con relojito en la cabecera de la receta |
| `src/ui/categoria.ts`, `src/ui/tag.ts`, `src/ui/resultados.ts` | Reciben orden y filtro |
| `src/main.ts` | Estado `duracionesActivas` y `orden`; acciones `elegir-duracion`, `filtrar-duracion`, `ordenar` |
| `src/ui/tokens.css`, `src/ui/base.css` | Estilos |
| `skills/recetario/SKILL.md` y documentos | Task 7 |

---

### Task 1: Los cinco valores, contar, filtrar y ordenar

**Files:**
- Modify: `src/recipe.ts`, `src/catalogo.ts`
- Test: `tests/duracion.test.ts` (nuevo), `tests/texto-receta.test.ts` (si su test de contexto usa `40 min`, pasarlo a `~30 min`)

**Interfaces:**
- Produces:
  - `export const DURACIONES = ['~15 min', '~30 min', '~60 min', '>60 min', '>1 día'] as const;` y `export type Duracion = (typeof DURACIONES)[number];` en `recipe.ts`, reexportados por `catalogo.ts`.
  - `export function duracionValida(valor: unknown): Duracion | ''` (en `recipe.ts`, reexportada).
  - En `catalogo.ts`: `export type Orden = 'alfa' | 'duracion';`, `ordenarRecetas(entradas: Entrada[], orden: Orden = 'alfa'): Entrada[]`, `contarDuraciones(entradas: Entrada[]): { valor: Duracion; cantidad: number }[]`, `filtrarPorDuracion(entradas: Entrada[], activas: string[]): Entrada[]`.

- [ ] **Step 1: Tests que fallan** — `tests/duracion.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import {
  DURACIONES, duracionValida, entradaDesdeFila, filaDesde, ordenarRecetas, contarDuraciones, filtrarPorDuracion
} from '../src/catalogo.js';
import { contextoDe, parse } from '../src/recipe.js';
import { entradaFalsa } from './dobles.js';

describe('los cinco valores de duración', () => {
  it('son estos, en este orden', () => {
    expect(DURACIONES).toEqual(['~15 min', '~30 min', '~60 min', '>60 min', '>1 día']);
  });

  it('duracionValida acepta los cinco tal cual y nada más', () => {
    for (const d of DURACIONES) expect(duracionValida(d)).toBe(d);
    for (const x of ['55 min', '30 min', '~30', '1 h', '', null, undefined, 30]) expect(duracionValida(x)).toBe('');
    expect(duracionValida('  ~30 min  ')).toBe('~30 min');
  });

  it('una fila del índice con un tiempo inválido llega sin duración', () => {
    const fila = filaDesde({ titulo: 'Baba', tiempo: '55 min' }, { id: 'f1' });
    expect(entradaDesdeFila(fila).tiempo).toBe('');
    const buena = filaDesde({ titulo: 'Baba', tiempo: '~60 min' }, { id: 'f1' });
    expect(entradaDesdeFila(buena).tiempo).toBe('~60 min');
  });

  it('contextoDe no muestra un tiempo inválido', () => {
    expect(contextoDe(parse('---\ntitulo: A\ntiempo: 55 min\n---\n'), 'Carnes')).toBe('Carnes');
    expect(contextoDe(parse('---\ntitulo: A\ntiempo: ~30 min\n---\n'), 'Carnes')).toBe('Carnes · ~30 min');
  });
});

describe('ordenar por duración', () => {
  const e = (titulo: string, tiempo: string, fav = false) =>
    entradaFalsa({ id_archivo: titulo, titulo, tiempo, tags: fav ? ['favorito'] : [] });
  const lista = [e('Zapallo', ''), e('Budín', '>1 día'), e('Arroz', '~30 min', true), e('Caldo', '~15 min'), e('Asado', '')];

  it('A–Z no cambia: favoritas primero y alfabético, sin mirar la duración', () => {
    expect(ordenarRecetas(lista).map(x => x.titulo)).toEqual(['Arroz', 'Asado', 'Budín', 'Caldo', 'Zapallo']);
    expect(ordenarRecetas(lista, 'alfa').map(x => x.titulo)).toEqual(['Arroz', 'Asado', 'Budín', 'Caldo', 'Zapallo']);
  });

  it('por duración mezcla las favoritas y deja las sin duración al final, en alfabético', () => {
    expect(ordenarRecetas(lista, 'duracion').map(x => x.titulo)).toEqual(['Caldo', 'Arroz', 'Budín', 'Asado', 'Zapallo']);
  });

  it('dentro de un mismo valor, alfabético', () => {
    const dos = [e('Pizza', '~30 min'), e('Arroz', '~30 min')];
    expect(ordenarRecetas(dos, 'duracion').map(x => x.titulo)).toEqual(['Arroz', 'Pizza']);
  });
});

describe('contar y filtrar por duración', () => {
  const lista = [
    entradaFalsa({ id_archivo: '1', tiempo: '~30 min' }),
    entradaFalsa({ id_archivo: '2', tiempo: '~30 min' }),
    entradaFalsa({ id_archivo: '3', tiempo: '>1 día' }),
    entradaFalsa({ id_archivo: '4', tiempo: '55 min' }),
    entradaFalsa({ id_archivo: '5', tiempo: '' })
  ];

  it('cuenta sólo los valores válidos, en el orden de DURACIONES, sin los que no tienen recetas', () => {
    expect(contarDuraciones(lista)).toEqual([{ valor: '~30 min', cantidad: 2 }, { valor: '>1 día', cantidad: 1 }]);
  });

  it('sin activas no filtra; con varias, las suma', () => {
    expect(filtrarPorDuracion(lista, [])).toHaveLength(5);
    expect(filtrarPorDuracion(lista, ['~30 min']).map(x => x.id_archivo)).toEqual(['1', '2']);
    expect(filtrarPorDuracion(lista, ['~30 min', '>1 día']).map(x => x.id_archivo)).toEqual(['1', '2', '3']);
  });
});
```

- [ ] **Step 2: Correrlos y ver que fallan**

Run: `npx vitest run tests/duracion.test.ts` — Expected: FAIL (no existen las exportaciones).

- [ ] **Step 3: Implementar**

`src/recipe.ts`, cerca de `CLAVES`:

```ts
/**
 * La duración no es texto libre: es uno de estos cinco valores, escritos tal
 * cual en `tiempo` (P29). Cuenta el tiempo hasta comer, con reposo y horno.
 */
export const DURACIONES = ['~15 min', '~30 min', '~60 min', '>60 min', '>1 día'] as const;
export type Duracion = (typeof DURACIONES)[number];

/** El valor si es uno de los cinco; si no, vacío: lo demás se lee como sin duración. */
export function duracionValida(valor: unknown): Duracion | '' {
  if (typeof valor !== 'string') return '';
  const s = valor.trim();
  return (DURACIONES as readonly string[]).includes(s) ? s as Duracion : '';
}
```

`contextoDe`:

```ts
export function contextoDe(receta: Receta, categoria: string): string {
  return [categoria, receta.rinde, duracionValida(receta.tiempo), receta.dificultad].filter(Boolean).join(' · ');
}
```

`src/catalogo.ts`: importar `duracionValida`, `DURACIONES` y el tipo `Duracion` de `./recipe.js` y reexportarlos al lado de `DIFICULTADES`:

```ts
export { DURACIONES, duracionValida } from './recipe.js';
export type { Duracion } from './recipe.js';
```

En `entradaDesdeFila`: `tiempo: duracionValida(texto.tiempo),`. `filaDesde` no cambia.

Reemplazar `ordenarRecetas` y sumar las otras dos:

```ts
/** A–Z es el orden de siempre; `duracion` es el del conmutador de las listas (P29). */
export type Orden = 'alfa' | 'duracion';

/**
 * A–Z: las favoritas primero y, dentro de cada bloque, alfabético (P27).
 * Por duración: de la más corta a la más larga, con las favoritas mezcladas
 * —la estrella de la tarjeta ya las marca—, alfabético dentro de cada valor y
 * las que no tienen duración al final (P29).
 */
export function ordenarRecetas(entradas: Entrada[], orden: Orden = 'alfa'): Entrada[] {
  const lista = Array.isArray(entradas) ? entradas : [];
  const alfa = (a: Entrada, b: Entrada): number => a.titulo.localeCompare(b.titulo, 'es');
  if (orden === 'duracion') {
    const pos = (e: Entrada): number => {
      const d = duracionValida(e.tiempo);
      return d ? DURACIONES.indexOf(d) : DURACIONES.length;
    };
    return [...lista].sort((a, b) => pos(a) - pos(b) || alfa(a, b));
  }
  return [...lista].sort((a, b) => Number(esFavorita(b)) - Number(esFavorita(a)) || alfa(a, b));
}

/** Cuántas recetas hay con cada duración, en el orden de `DURACIONES`, sin los valores vacíos. */
export function contarDuraciones(entradas: Entrada[]): { valor: Duracion; cantidad: number }[] {
  const lista = Array.isArray(entradas) ? entradas : [];
  return DURACIONES
    .map(valor => ({ valor, cantidad: lista.filter(e => duracionValida(e.tiempo) === valor).length }))
    .filter(x => x.cantidad > 0);
}

/** Las recetas con alguna de las duraciones encendidas; sin ninguna encendida, todas. */
export function filtrarPorDuracion(entradas: Entrada[], activas: string[]): Entrada[] {
  const lista = Array.isArray(entradas) ? entradas : [];
  if (!activas.length) return lista;
  return lista.filter(e => activas.includes(duracionValida(e.tiempo)));
}
```

- [ ] **Step 4: Correr todo**

Run: `npx vitest run && npm run typecheck`
Expected: PASS. Si algún test existente arma una entrada del índice con `tiempo: '40 min'` o similar y espera verlo, pasarlo a un valor válido (`~30 min`) y anotarlo en el reporte.

- [ ] **Step 5: Commit local**

```bash
git add src/recipe.ts src/catalogo.ts tests/
git commit -m "La duración es uno de cinco valores: validar, contar, filtrar y ordenar"
```

---

### Task 2: Los relojitos

**Files:**
- Modify: `src/ui/iconos.ts`, `src/ui/tokens.css`
- Test: `tests/iconos-duracion.test.ts` (nuevo)

**Interfaces:**
- Consumes: `DURACIONES`, `Duracion` (Task 1).
- Produces: `export const ICONO_DE_DURACION: Record<Duracion, string>` en `iconos.ts`, cada valor un `<svg viewBox="0 0 24 24">…</svg>` armado con el helper `svg()` del archivo.

- [ ] **Step 1: Test que falla**

```ts
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import { ICONO_DE_DURACION } from '../src/ui/iconos.js';
import { DURACIONES } from '../src/catalogo.js';

const TOKENS = readFileSync(new URL('../src/ui/tokens.css', import.meta.url), 'utf8');

describe('los relojitos de duración', () => {
  it('hay uno por valor, y todos son distintos', () => {
    const iconos = DURACIONES.map(d => ICONO_DE_DURACION[d]);
    expect(iconos.every(i => i.startsWith('<svg viewBox="0 0 24 24">'))).toBe(true);
    expect(new Set(iconos).size).toBe(5);
  });

  it('>1 día son dos relojes: dos marcas de las 12', () => {
    expect(ICONO_DE_DURACION['>1 día'].match(/v1\.33"/g)).toHaveLength(2);
  });

  it('>60 min termina en flecha: el aro de afuera tiene trazo 1.6', () => {
    expect(ICONO_DE_DURACION['>60 min']).toContain('stroke-width="1.6"');
  });

  it('el disco que corta el reloj de atrás toma el fondo de donde esté', () => {
    expect(ICONO_DE_DURACION['>1 día']).toContain('style="fill:var(--fondo-reloj)"');
    expect(TOKENS).toContain('--fondo-reloj:');
  });
});
```

- [ ] **Step 2: Correrlo y ver que falla** — `npx vitest run tests/iconos-duracion.test.ts` → FAIL.

- [ ] **Step 3: Implementar** — en `src/ui/iconos.ts`, después de `ICO`, con import de tipo `import type { Duracion } from '../recipe.js';`:

```ts
/**
 * Los relojitos de la duración (P29): una aguja y el recorrido desde las 12,
 * tenue. `~60 min` completa la vuelta con la aguja de nuevo arriba; `>60 min`
 * suma un cuarto de aro que sigue, con flecha; `>1 día` son dos relojes. El
 * disco de `>1 día` corta al reloj de atrás con el fondo de donde se dibuje:
 * `--fondo-reloj`.
 */
export const ICONO_DE_DURACION: Record<Duracion, string> = {
  '~15 min': svg('<circle cx="12" cy="12" r="9"/><g opacity=".3"><path d="M12 12L12 3A9 9 0 0 1 21 12Z" fill="currentColor" stroke="none"/></g><path d="M12 3v2"/><path d="M12 12L18 12"/><circle cx="12" cy="12" r=".6" fill="currentColor"/>'),
  '~30 min': svg('<circle cx="12" cy="12" r="9"/><g opacity=".3"><path d="M12 12L12 3A9 9 0 0 1 12 21Z" fill="currentColor" stroke="none"/></g><path d="M12 3v2"/><path d="M12 12L12 18"/><circle cx="12" cy="12" r=".6" fill="currentColor"/>'),
  '~60 min': svg('<circle cx="12" cy="12" r="9"/><g opacity=".3"><circle cx="12" cy="12" r="9" fill="currentColor" stroke="none"/></g><path d="M12 3v2"/><path d="M12 12L12 6"/><circle cx="12" cy="12" r=".6" fill="currentColor"/>'),
  '>60 min': svg('<circle cx="12" cy="12" r="7"/><g opacity=".3"><circle cx="12" cy="12" r="7" fill="currentColor" stroke="none"/></g><path d="M12 5v1.56"/><path d="M12 12L12 7.33"/><circle cx="12" cy="12" r=".6" fill="currentColor"/><path d="M12 2A10 10 0 0 1 21.78 9.92" stroke-width="1.6"/><path d="M22.92 7.15L21.78 9.92L19.61 7.85" stroke-width="1.6"/>'),
  '>1 día': svg('<g opacity=".6"><circle cx="15.5" cy="8.5" r="6"/><g opacity=".3"><path d="M15.5 8.5L15.5 2.5A6 6 0 0 1 15.5 14.5Z" fill="currentColor" stroke="none"/></g><path d="M15.5 2.5v1.33"/><path d="M15.5 8.5L15.5 12.5"/><circle cx="15.5" cy="8.5" r=".6" fill="currentColor"/></g><circle cx="8.5" cy="15.5" r="7.2" style="fill:var(--fondo-reloj)" stroke="none"/><circle cx="8.5" cy="15.5" r="6"/><g opacity=".3"><path d="M8.5 15.5L8.5 9.5A6 6 0 0 1 14.5 15.5Z" fill="currentColor" stroke="none"/></g><path d="M8.5 9.5v1.33"/><path d="M8.5 15.5L12.5 15.5"/><circle cx="8.5" cy="15.5" r=".6" fill="currentColor"/>')
};
```

`src/ui/tokens.css`, en `:root`, junto a `--ico`:

```css
  /* P29 — el fondo con el que `>1 día` corta el reloj de atrás. Cada lugar
     que dibuja un relojito sobre otro fondo lo redefine. */
  --fondo-reloj: var(--surface);
```

- [ ] **Step 4: Correr todo** — `npx vitest run && npm run typecheck` → PASS.

- [ ] **Step 5: Commit local**

```bash
git add src/ui/iconos.ts src/ui/tokens.css tests/iconos-duracion.test.ts
git commit -m "Los cinco relojitos de la duración"
```

---

### Task 3: El editor

**Files:**
- Modify: `src/ui/editor.ts`, `src/main.ts`, `src/ui/base.css`
- Test: `tests/vista-editor.test.ts`, `tests/main-rutas.test.ts` (o el archivo de tests del editor que ya cubre `tag-especial`)

**Interfaces:**
- Consumes: `DURACIONES`, `duracionValida` (Task 1); `ICONO_DE_DURACION` (Task 2).
- Produces: botones `data-accion="elegir-duracion" data-valor="<valor>" aria-pressed`, y `<input type="hidden" name="tiempo">`.

- [ ] **Step 1: Tests que fallan**

En `tests/vista-editor.test.ts`, reemplazar el test «rinde y tiempo no sugieren un valor: son texto libre» por:

```ts
it('rinde sigue siendo texto libre, a lo ancho', () => {
  const html = renderEditor({ entrada: null, receta: parse('---\ntitulo: A\n---\n'), categorias });
  expect(html).toContain('<input name="rinde" value="">');
  expect(html).not.toContain('<input name="tiempo"');
});

it('la duración son cinco botones con su relojito, y el cargado va apretado', () => {
  const html = renderEditor({ entrada: null, receta: parse('---\ntitulo: A\ntiempo: ~60 min\n---\n'), categorias });
  for (const d of DURACIONES) expect(html).toContain(`data-accion="elegir-duracion" data-valor="${escapar(d)}"`);
  expect(html).toContain(`data-valor="~60 min" aria-pressed="true"`);
  expect(html.match(/aria-pressed="true"/g)?.length).toBeGreaterThanOrEqual(1);
  expect(html).toContain(ICONO_DE_DURACION['~15 min']);
  expect(html).toContain('<input type="hidden" name="tiempo" value="~60 min">');
  expect(html).toContain('Hasta comer, con reposo y horno incluidos.');
});

it('un tiempo inválido abre sin ningún botón de duración apretado', () => {
  const html = renderEditor({ entrada: null, receta: parse('---\ntitulo: A\ntiempo: 55 min\n---\n'), categorias });
  const bloque = html.slice(html.indexOf('data-duraciones'), html.indexOf('name="tiempo"'));
  expect(bloque).not.toContain('aria-pressed="true"');
  expect(html).toContain('<input type="hidden" name="tiempo" value="">');
});

it('del formulario sale sólo un valor válido', () => {
  const base = parse('---\ntitulo: A\ntiempo: 55 min\n---\n');
  expect(recetaDesdeFormulario({ tiempo: '~30 min' }, base).tiempo).toBe('~30 min');
  expect(recetaDesdeFormulario({ tiempo: '' }, base).tiempo).toBeNull();
  expect(recetaDesdeFormulario({ tiempo: '45 min' }, base).tiempo).toBeNull();
});
```

(Importar `DURACIONES` de `../src/catalogo.js`, `ICONO_DE_DURACION` de `../src/ui/iconos.js` y `escapar` de `../src/ui/markdown.js`. En el test de la línea 38, que recorre las claves del formulario, `tiempo` sigue estando: ahora es el `hidden`.)

En el archivo donde se prueba la acción `tag-especial` en `main` (buscar `tag-especial` en `tests/`), con los mismos helpers:

```ts
it('elegir una duración aprieta ese botón, suelta los otros y la escribe en el campo oculto; tocarla de nuevo la saca', async () => {
  const { abrir, tocar, app } = await montar();
  await abrir('#/nueva');
  await tocar('elegir-duracion', { valor: '~30 min' });
  expect(app.innerHTML).toContain('data-valor="~30 min" aria-pressed="true"');
  expect(app.innerHTML).toContain('name="tiempo" value="~30 min"');
  await tocar('elegir-duracion', { valor: '>1 día' });
  expect(app.innerHTML).toContain('data-valor="~30 min" aria-pressed="false"');
  expect(app.innerHTML).toContain('data-valor="&gt;1 día" aria-pressed="true"');
  await tocar('elegir-duracion', { valor: '>1 día' });
  expect(app.innerHTML).toContain('name="tiempo" value=""');
});
```

Si el DOM falso no refleja `input.value` en `innerHTML`, verificar el campo con `querySelector('input[name="tiempo"]').value` o con el atributo `value` que setee la acción (ver Step 3: se setean los dos), y anotarlo.

- [ ] **Step 2: Correrlos y ver que fallan.**

- [ ] **Step 3: Implementar**

`src/ui/editor.ts` — import `DURACIONES, duracionValida` de `../catalogo.js` e `ICONO_DE_DURACION` de `./iconos.js`. Función nueva:

```ts
/**
 * La duración: cinco botones con su relojito, uno apretado a la vez (P29).
 * Tocar el apretado lo suelta. El valor viaja en el `hidden`, que es lo que
 * lee el formulario y lo que compara «cambios sin guardar».
 */
function campoDuracion(tiempo: string | null): string {
  const actual = duracionValida(tiempo);
  const botones = DURACIONES.map(d =>
    `<button type="button" class="dur-btn" data-accion="elegir-duracion" data-valor="${escapar(d)}" aria-pressed="${d === actual}">` +
    `${ICONO_DE_DURACION[d]}${escapar(d)}</button>`).join('');
  return '<div class="campo" data-duraciones><span>Duración</span>' +
    `<div class="duraciones" role="group" aria-label="Duración">${botones}</div>` +
    `<input type="hidden" name="tiempo" value="${escapar(actual)}">` +
    '<p class="aviso-mudo">Hasta comer, con reposo y horno incluidos.</p>' +
  '</div>';
}
```

En `renderEditor`, reemplazar el bloque `.par` de rinde y tiempo por:

```ts
    campo('rinde', 'Rinde', receta.rinde) +
    campoDuracion(receta.tiempo) +
```

En `recetaDesdeFormulario`: `tiempo: duracionValida(datos['tiempo']) || null,`.

`src/main.ts`, junto a la acción `tag-especial`:

```ts
  if (accion === 'elegir-duracion') {
    const valor = boton.dataset['valor'] ?? '';
    const puesto = boton.getAttribute('aria-pressed') !== 'true';
    for (const b of document.querySelectorAll<HTMLElement>('#app [data-accion="elegir-duracion"]')) {
      b.setAttribute('aria-pressed', String(puesto && b === boton));
    }
    const oculto = document.querySelector<HTMLInputElement>('#app input[name="tiempo"]');
    if (oculto) { oculto.value = puesto ? valor : ''; oculto.setAttribute('value', oculto.value); }
    return;
  }
```

`src/ui/base.css`, en la sección del editor, después de `.tag-esp`:

```css
/* La duración (P29): cinco botones con relojito, en tres columnas. Apretado se
   invierte, como los tags especiales; el reloj de `>1 día` corta con el fondo
   del botón. */
.duraciones { display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--e-2); }
.dur-btn { --fondo-reloj: var(--surface-alta); min-height: 72px; padding: var(--e-2) var(--e-1);
           display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 6px;
           background: var(--surface-alta); color: var(--fg-2); border: 1px solid var(--borde);
           border-radius: var(--r-medio); font: 600 var(--txt-chico)/1.2 var(--tipo); cursor: pointer; }
.dur-btn svg { width: 24px; height: 24px; color: var(--fg); stroke: currentColor; fill: none;
               stroke-width: 1.5; stroke-linecap: round; stroke-linejoin: round; }
.dur-btn[aria-pressed="true"] { --fondo-reloj: var(--fg); background: var(--fg); color: var(--bg); border-color: var(--fg); }
.dur-btn[aria-pressed="true"] svg { color: var(--bg); }
[data-duraciones] .aviso-mudo { margin: var(--e-2) 0 0; }
```

Si `.aviso-mudo` no existe como clase, usar la que ya dibuja la leyenda de `incompleta` (buscarla en `base.css`) y anotarlo.

- [ ] **Step 4: Correr todo** — `npx vitest run && npm run typecheck && npm run build` → PASS.

- [ ] **Step 5: Commit local**

```bash
git add src/ui/editor.ts src/main.ts src/ui/base.css tests/
git commit -m "La duración se elige en el editor con cinco botones"
```

---

### Task 4: Dónde se ve

**Files:**
- Modify: `src/ui/componentes.ts` (`tarjeta`), `src/ui/fichas-receta.ts` (`fichaCabecera`), `src/ui/tokens.css`, `src/ui/base.css`
- Test: `tests/componentes.test.ts`, `tests/vista-receta.test.ts`

**Interfaces:**
- Consumes: `duracionValida` (Task 1), `ICONO_DE_DURACION` (Task 2).
- Produces: `export function duracionConReloj(tiempo: unknown): string` en `componentes.ts` —`<span class="dur">svg + valor</span>` o vacío—, que usan `tarjeta` y `fichaCabecera`.

- [ ] **Step 1: Tests que fallan**

`tests/componentes.test.ts`, reemplazar el primer test de `tarjeta` y sumar:

```ts
it('lleva foto, título y la línea de contexto con categoría y duración, con su relojito', () => {
  const html = tarjeta(entradaFalsa({ titulo: 'Rabas', categoria: 'Pescados y mariscos', tiempo: '~30 min', rinde: '4 porciones' }));
  expect(html).toContain('Rabas');
  expect(html).toContain(`Pescados y mariscos · <span class="dur">${ICO_DUR['~30 min']}~30 min</span> · 4 porciones`);
});

it('sin duración válida, la línea no la nombra', () => {
  const html = tarjeta(entradaFalsa({ categoria: 'Carnes', tiempo: '55 min', rinde: '4' }));
  expect(html).toContain('Carnes · 4');
  expect(html).not.toContain('class="dur"');
});

it('con motivo, la duración va después del motivo', () => {
  const html = tarjeta(entradaFalsa({ tiempo: '>60 min' }), { motivo: 'tiene tag horno' });
  expect(html).toContain(`<span class="motivo">tiene tag horno</span> · <span class="dur">${ICO_DUR['>60 min']}&gt;60 min</span>`);
});
```

(`import { ICONO_DE_DURACION as ICO_DUR } from '../src/ui/iconos.js';`.) Revisar el test «la línea de contexto sin marca» que corta en `'</span></span>'`: con el `span.ctx-txt` nuevo, ajustar el corte al cierre real de `.ctx` sin debilitar lo que prueba.

`tests/vista-receta.test.ts`:

```ts
it('la línea de contexto de la receta lleva la duración con su relojito', () => {
  const r = parse('---\ntitulo: Pan\nrinde: 4\ntiempo: ~60 min\n---\n');
  const html = renderReceta({ entrada: entradaFalsa({ categoria: 'Panes y masas' }), receta: r });
  expect(html).toContain(`<span class="dur">${ICONO_DE_DURACION['~60 min']}~60 min</span>`);
});

it('un tiempo inválido no aparece en la receta', () => {
  const r = parse('---\ntitulo: Pan\ntiempo: 55 min\n---\n');
  expect(renderReceta({ entrada: entradaFalsa(), receta: r })).not.toContain('55 min');
});
```

- [ ] **Step 2: Correrlos y ver que fallan.**

- [ ] **Step 3: Implementar**

`src/ui/componentes.ts` — imports: `duracionValida` de `../catalogo.js`, `ICONO_DE_DURACION` de `./iconos.js`.

```ts
/** La duración con su relojito, o nada si el tiempo no es uno de los cinco valores (P29). */
export function duracionConReloj(tiempo: unknown): string {
  const d = duracionValida(tiempo);
  return d ? `<span class="dur">${ICONO_DE_DURACION[d]}${escapar(d)}</span>` : '';
}
```

En `tarjeta`, la línea de contexto pasa a un solo `span` de texto —`.ctx` es flex, y el relojito partiría el texto en varios ítems—:

```ts
  const dur = duracionConReloj(e.tiempo);
  const contexto = motivo
    ? `<span class="ctx-txt"><span class="motivo">${escapar(motivo)}</span>${dur ? ` · ${dur}` : ''}</span>`
    : `<span class="pin" style="background:${colorCategoria(e.categoria)}"></span>` +
      `<span class="ctx-txt">${[escapar(e.categoria), dur, escapar(e.rinde)].filter(Boolean).join(' · ')}</span>`;
```

`src/ui/fichas-receta.ts` — `fichaCabecera` arma la línea con las mismas piezas en vez de `escapar(contexto)` (import de `duracionConReloj` desde `./componentes.js`; `componentes.ts` no importa `fichas-receta.ts`, así que no hay ciclo):

```ts
  const partes = [escapar(categoria), escapar(receta.rinde ?? ''), duracionConReloj(receta.tiempo), escapar(receta.dificultad ?? '')]
    .filter(Boolean);
  ...
    (partes.length
      ? `<div class="rec-ctx">${categoria && pin ? `<span class="pin" style="background:${colorCategoria(categoria)}"></span>` : ''}<span class="ctx-txt">${partes.join(' · ')}</span></div>`
      : '') +
```

y `contextoDe` deja de usarse ahí (sigue en PDF y texto). Borrar el import si queda sin uso.

`src/ui/tokens.css`, en §6.1 Tarjeta:

```css
/* P29 — la duración va en la misma línea de texto, con el relojito pegado. */
.ctx-txt { min-width: 0; }
.dur { white-space: nowrap; }
.dur svg { width: 15px; height: 15px; vertical-align: -3px; margin-right: 3px; stroke: currentColor; fill: none;
           stroke-width: 1.5; stroke-linecap: round; stroke-linejoin: round; }
```

El fondo de la tarjeta y de la ficha es `--surface`, que ya es el valor por defecto de `--fondo-reloj`.

- [ ] **Step 4: Correr todo** — `npx vitest run && npm run typecheck && npm run build` → PASS.

- [ ] **Step 5: Commit local**

```bash
git add src/ tests/
git commit -m "La duración con su relojito en la tarjeta, la búsqueda y la receta"
```

---

### Task 5: Filtrar y ordenar en la categoría y la lista por tag

**Files:**
- Modify: `src/ui/componentes.ts`, `src/ui/categoria.ts`, `src/ui/tag.ts`, `src/main.ts`, `src/ui/base.css`
- Test: `tests/componentes.test.ts`, `tests/vista-categoria.test.ts` (o el que pruebe `renderCategoria`), `tests/vista-tag.test.ts`, `tests/main-rutas.test.ts`

**Interfaces:**
- Consumes: `contarDuraciones`, `filtrarPorDuracion`, `ordenarRecetas(entradas, orden)`, `Orden`, `Duracion` (Task 1); `ICONO_DE_DURACION` (Task 2).
- Produces:
  - `filaDuraciones(conteo: { valor: Duracion; cantidad: number }[], activas: string[]): string` — chips `data-accion="filtrar-duracion" data-valor`.
  - `conmutadorOrden(orden: Orden): string` — botones `data-accion="ordenar" data-valor="alfa|duracion"`.
  - `OpcionesCategoria` y `OpcionesTag` suman `duraciones: { valor: Duracion; cantidad: number }[]`, `duracionesActivas: string[]`, `orden: Orden`.

- [ ] **Step 1: Tests que fallan**

`tests/componentes.test.ts`:

```ts
describe('la fila de duraciones y el conmutador de orden', () => {
  it('un chip por valor con su relojito y cantidad; el encendido va activo', () => {
    const html = filaDuraciones([{ valor: '~15 min', cantidad: 2 }, { valor: '>1 día', cantidad: 1 }], ['>1 día']);
    expect(html).toContain('data-accion="filtrar-duracion" data-valor="~15 min"');
    expect(html).toContain(ICONO_DE_DURACION['~15 min']);
    expect(html).toContain('<span class="cuenta">2</span>');
    expect(html).toMatch(/class="chip act" data-accion="filtrar-duracion" data-valor="&gt;1 día"/);
  });

  it('un valor encendido sin recetas se dibuja igual, para poder apagarlo', () => {
    expect(filaDuraciones([], ['~30 min'])).toContain('data-valor="~30 min"');
  });

  it('sin valores ni encendidos, no hay fila', () => {
    expect(filaDuraciones([], [])).toBe('');
  });

  it('el conmutador marca el orden elegido', () => {
    const html = conmutadorOrden('duracion');
    expect(html).toContain('data-accion="ordenar" data-valor="alfa" aria-pressed="false"');
    expect(html).toContain('data-accion="ordenar" data-valor="duracion" aria-pressed="true"');
    expect(html).toContain('A–Z');
    expect(html).toContain('Duración');
  });
});
```

Tests de pantalla (en el archivo que ya prueba `renderCategoria`; buscarlo con `grep -l renderCategoria tests`):

```ts
const base = { nombre: 'Pescados y mariscos', total: 2, visibles: 2, tagsActivos: [], tags: [], duracionesActivas: [], orden: 'alfa' as const };
const con = [
  entradaFalsa({ id_archivo: 'a', titulo: 'Zarzuela', tiempo: '~15 min' }),
  entradaFalsa({ id_archivo: 'b', titulo: 'Abadejo', tiempo: '>60 min' })
];

it('con duraciones, dibuja la fila de filtro y el conmutador', () => {
  const html = renderCategoria({ ...base, entradas: con, duraciones: [{ valor: '~15 min', cantidad: 1 }, { valor: '>60 min', cantidad: 1 }] });
  expect(html).toContain('data-accion="filtrar-duracion"');
  expect(html).toContain('data-accion="ordenar"');
});

it('sin ninguna duración, no hay fila de filtro ni conmutador', () => {
  const html = renderCategoria({ ...base, entradas: con, duraciones: [] });
  expect(html).not.toContain('data-accion="filtrar-duracion"');
  expect(html).not.toContain('data-accion="ordenar"');
});

it('ordena según el orden elegido', () => {
  const alfa = renderCategoria({ ...base, entradas: con, duraciones: [{ valor: '~15 min', cantidad: 1 }] });
  expect(alfa.indexOf('Abadejo')).toBeLessThan(alfa.indexOf('Zarzuela'));
  const dur = renderCategoria({ ...base, entradas: con, orden: 'duracion', duraciones: [{ valor: '~15 min', cantidad: 1 }] });
  expect(dur.indexOf('Zarzuela')).toBeLessThan(dur.indexOf('Abadejo'));
});
```

Lo mismo para `renderTag` en `tests/vista-tag.test.ts` (sumar `duraciones: [], duracionesActivas: [], orden: 'alfa'` al `base` existente y un test de que dibuja las dos filas con duraciones).

`tests/main-rutas.test.ts` (con los helpers `montar`, `abrir`, `tocar`, `estado`, `storeFake`):

```ts
it('en la categoría, encender duraciones suma y el total baja; cambiar de pantalla lo limpia', async () => {
  const original = storeFake.buscar;
  storeFake.buscar = () => [
    entradaFalsa({ id_archivo: 'f1', titulo: 'Rabas', categoria: 'Carnes', tiempo: '~30 min' }),
    entradaFalsa({ id_archivo: 'f2', titulo: 'Guiso', categoria: 'Carnes', tiempo: '>60 min' }),
    entradaFalsa({ id_archivo: 'f3', titulo: 'Asado', categoria: 'Carnes', tiempo: '' })
  ];
  try {
    const { abrir, tocar, app } = await montar();
    await abrir('#/c/Carnes');
    await tocar('filtrar-duracion', { valor: '~30 min' });
    expect(app.innerHTML).toContain('Rabas');
    expect(app.innerHTML).not.toContain('Guiso');
    expect(app.innerHTML).toContain('<span class="tot">1</span>');
    await tocar('filtrar-duracion', { valor: '>60 min' });
    expect(app.innerHTML).toContain('Guiso');
    expect(app.innerHTML).not.toContain('Asado');
    await abrir('#/');
    await abrir('#/c/Carnes');
    expect(app.innerHTML).toContain('Asado');
  } finally {
    storeFake.buscar = original;
  }
});

it('ordenar por duración reordena la lista y vuelve a A–Z al cambiar de pantalla', async () => {
  const original = storeFake.buscar;
  storeFake.buscar = () => [
    entradaFalsa({ id_archivo: 'f1', titulo: 'Asado', categoria: 'Carnes', tiempo: '>60 min' }),
    entradaFalsa({ id_archivo: 'f2', titulo: 'Rabas', categoria: 'Carnes', tiempo: '~15 min' })
  ];
  try {
    const { abrir, tocar, app } = await montar();
    await abrir('#/c/Carnes');
    expect(app.innerHTML.indexOf('Asado')).toBeLessThan(app.innerHTML.indexOf('Rabas'));
    await tocar('ordenar', { valor: 'duracion' });
    expect(app.innerHTML.indexOf('Rabas')).toBeLessThan(app.innerHTML.indexOf('Asado'));
    await abrir('#/');
    await abrir('#/c/Carnes');
    expect(app.innerHTML.indexOf('Asado')).toBeLessThan(app.innerHTML.indexOf('Rabas'));
  } finally {
    storeFake.buscar = original;
  }
});
```

Adaptar `abrir('#/')` al modo en que los tests existentes cambian de pantalla, si es otro.

- [ ] **Step 2: Correrlos y ver que fallan.**

- [ ] **Step 3: Implementar**

`src/ui/componentes.ts`:

```ts
/**
 * Los chips de duración de una lista (P29): cada uno con su relojito y
 * cuántas recetas trae. Un valor encendido se dibuja aunque ya no traiga
 * ninguna, para poder apagarlo. Sin nada que mostrar, no hay fila.
 */
export function filaDuraciones(conteo: { valor: Duracion; cantidad: number }[], activas: string[]): string {
  const lista = DURACIONES
    .map(valor => ({ valor, cantidad: conteo.find(c => c.valor === valor)?.cantidad ?? 0 }))
    .filter(x => x.cantidad > 0 || activas.includes(x.valor));
  if (!lista.length) return '';
  const chips = lista.map(({ valor, cantidad }) =>
    `<button class="chip${activas.includes(valor) ? ' act' : ''}" data-accion="filtrar-duracion" data-valor="${escapar(valor)}">` +
    `${ICONO_DE_DURACION[valor]}${escapar(valor)}<span class="cuenta">${cantidad}</span></button>`).join('');
  return `<div class="fila-dur" role="group" aria-label="Filtrar por duración">${chips}</div>`;
}

/** «A–Z | Duración», a la derecha (P29). */
export function conmutadorOrden(orden: Orden): string {
  const b = (valor: Orden, contenido: string) =>
    `<button data-accion="ordenar" data-valor="${valor}" aria-pressed="${orden === valor}">${contenido}</button>`;
  return '<div class="orden"><div class="orden-seg" role="group" aria-label="Orden">' +
    b('alfa', 'A–Z') + b('duracion', `${ICONO_DE_DURACION['~30 min']}Duración`) +
  '</div></div>';
}
```

(imports: `DURACIONES`, `type Duracion`, `type Orden` de `../catalogo.js`.)

`src/ui/categoria.ts`: sumar a `OpcionesCategoria` `duraciones`, `duracionesActivas`, `orden`. En el render:

```ts
  const hayDuraciones = duraciones.length > 0 || duracionesActivas.length > 0;
  const filtroDuracion = filaDuraciones(duraciones, duracionesActivas);
  const orden_ = hayDuraciones ? conmutadorOrden(orden) : '';
  const lista = ordenarRecetas(entradas, orden).map(e => tarjeta(e)).join('');
  ...
  const hayFiltros = activos.length > 0 || duracionesActivas.length > 0;
  // el vacío con filtros usa `hayFiltros` en vez de `activos.length`
  return encabezado({ titulo: nombre, volver: true, total }) +
    `<div class="cuerpo denso">${filtros}${filtroDuracion}${orden_}${cuerpo}</div>`;
```

Donde el texto del vacío con filtros hoy dice «Ninguna receta con esos tags…», dejar la misma frase si sólo hay tags, y para duraciones o ambos: «Ninguna receta con esos filtros. Probá sacando alguno de los filtros de arriba.» (el spec §6 manda «Probá sacando alguno de los filtros de arriba.»).

`src/ui/tag.ts`: igual —`filaDuraciones` y `conmutadorOrden` después del carrusel, `ordenarRecetas(entradas, orden)`—.

`src/main.ts`:

```ts
let duracionesActivas: string[] = [];   // filtro de duración de la categoría o la lista por tag (P29)
let orden: Orden = 'alfa';               // el conmutador de las listas; vuelve a A–Z al cambiar de pantalla
```

En el bloque `if (cambiaDePantalla)`, junto a `tagsActivos = [];`: `duracionesActivas = []; orden = 'alfa';`.

`case 'categoria'`:

```ts
      const porTags = store.buscar({ categoria: nombre, tags: tagsActivos });
      const entradas = ordenarRecetas(filtrarPorDuracion(porTags, duracionesActivas), orden);
      pintar(renderCategoria({
        nombre, entradas: entradas.slice(0, visibles), total: entradas.length,
        visibles: Math.min(visibles, entradas.length), tagsActivos, tags: store.tagsDe(nombre),
        duraciones: contarDuraciones(porTags), duracionesActivas, orden
      }));
```

`case 'tag'`: lo mismo con `store.buscar({ tags: activos })`.

Acciones, junto al manejo de `data-tag`:

```ts
  if (accion === 'filtrar-duracion') {
    const valor = boton.dataset['valor'] ?? '';
    duracionesActivas = duracionesActivas.includes(valor)
      ? duracionesActivas.filter(d => d !== valor) : [...duracionesActivas, valor];
    visibles = TRAMO;
    return render();
  }
  if (accion === 'ordenar') {
    orden = boton.dataset['valor'] === 'duracion' ? 'duracion' : 'alfa';
    visibles = TRAMO;
    return render();
  }
```

`src/ui/base.css`, junto al carrusel:

```css
/* P29 — la fila de duraciones: chips con relojito que se deslizan como el
   carrusel, sin degradé ni flechas; y el conmutador de orden, a la derecha. */
.fila-dur { display: flex; gap: var(--e-2); overflow-x: auto; scrollbar-width: none;
            margin-inline: calc(-1 * var(--e-4)); padding-inline: var(--e-4); }
.fila-dur::-webkit-scrollbar { display: none; }
.fila-dur .chip { flex: 0 0 auto; --fondo-reloj: var(--surface-alta); }
.fila-dur .chip.act { --fondo-reloj: var(--acento-suave); }
.orden { display: flex; justify-content: flex-end; }
.orden-seg { display: inline-grid; grid-template-columns: auto auto; border: 1px solid var(--borde);
             border-radius: var(--r-medio); overflow: hidden; }
.orden-seg button { min-height: 40px; padding: 0 var(--e-3); border: 0; background: none; color: var(--fg-2);
                    font: 600 var(--txt-chico)/1 var(--tipo); cursor: pointer; display: inline-flex;
                    align-items: center; gap: 6px; --fondo-reloj: var(--bg); }
.orden-seg button + button { border-left: 1px solid var(--borde); }
.orden-seg button[aria-pressed="true"] { background: var(--fg); color: var(--bg); --fondo-reloj: var(--fg); }
.orden-seg svg { width: 16px; height: 16px; stroke: currentColor; fill: none; stroke-width: 1.5;
                 stroke-linecap: round; stroke-linejoin: round; }
```

Verificar en `tokens.css` cuál es el fondo de `.chip` y de `.chip.act` y usar esos tokens para `--fondo-reloj` si difieren de los de arriba.

- [ ] **Step 4: Correr todo** — `npx vitest run && npm run typecheck && npm run build` → PASS.

- [ ] **Step 5: Commit local**

```bash
git add src/ tests/
git commit -m "Filtrar y ordenar por duración en la categoría y la lista por tag"
```

---

### Task 6: Ordenar en la búsqueda

**Files:**
- Modify: `src/ui/resultados.ts`, `src/main.ts`
- Test: `tests/vista-resultados.test.ts` (o el que pruebe `renderResultados`), `tests/main-rutas.test.ts`

**Interfaces:**
- Consumes: `ordenarRecetas(entradas, orden)`, `Orden` (Task 1); `conmutadorOrden` (Task 5); estado `orden` de `main.ts` (Task 5).
- Produces: `OpcionesResultados` suma `orden?: Orden` (por defecto `'alfa'`).

- [ ] **Step 1: Tests que fallan**

```ts
const e = (id: string, titulo: string, tiempo: string) => entradaFalsa({ id_archivo: id, titulo, tiempo });

it('con alguna duración, el conmutador va arriba de los grupos', () => {
  const html = renderResultados({ consulta: 'horno', grupos: { porNombre: [e('1', 'Besugo al horno', '~60 min')], porIngrediente: [], porTag: [] } });
  expect(html).toContain('data-accion="ordenar"');
  expect(html.indexOf('data-accion="ordenar"')).toBeLessThan(html.indexOf('grupo-res'));
});

it('sin ninguna duración, no hay conmutador', () => {
  const html = renderResultados({ consulta: 'horno', grupos: { porNombre: [e('1', 'Besugo', '')], porIngrediente: [], porTag: [] } });
  expect(html).not.toContain('data-accion="ordenar"');
});

it('ordena por duración dentro de cada grupo, sin mezclarlos', () => {
  const html = renderResultados({
    consulta: 'horno', orden: 'duracion',
    grupos: {
      porNombre: [e('1', 'Besugo al horno', '>60 min'), e('2', 'Pollo al horno', '~30 min')],
      porIngrediente: [],
      porTag: [{ entrada: e('3', 'Arroz', '~15 min'), motivo: 'tiene tag horno' }]
    }
  });
  expect(html.indexOf('Pollo al horno')).toBeLessThan(html.indexOf('Besugo al horno'));
  expect(html.indexOf('Besugo al horno')).toBeLessThan(html.indexOf('Arroz'));
});
```

En `tests/main-rutas.test.ts`: con `storeFake.buscarPorTexto` devolviendo dos recetas por nombre con duraciones distintas, abrir `#/buscar?q=…` (ver la ruta real en `router.ts`), `tocar('ordenar', { valor: 'duracion' })` y verificar que se invirtió el orden.

- [ ] **Step 2: Correrlos y ver que fallan.**

- [ ] **Step 3: Implementar**

`src/ui/resultados.ts`:

```ts
export interface OpcionesResultados {
  consulta: string;
  grupos: Coincidencias;
  /** El conmutador de orden (P29): ordena dentro de cada grupo. */
  orden?: Orden;
}
```

`conMotivoOrdenado(cs, orden)` pasa `orden` a `ordenarRecetas`. En `renderResultados({ consulta, grupos, orden = 'alfa' })`:

```ts
  const todas = [...porNombre, ...porIngrediente.map(c => c.entrada), ...porTag.map(c => c.entrada)];
  const conmutador = todas.some(e => duracionValida(e.tiempo)) ? conmutadorOrden(orden) : '';
  const cuerpo =
    grupo('Por nombre', ordenarRecetas(porNombre, orden).map(e => tarjeta(e))) +
    grupo('Por ingrediente', conMotivoOrdenado(porIngrediente, orden)) +
    grupo('Por tag', conMotivoOrdenado(porTag, orden));
  ...
  return caja + (cuerpo ? `<div class="cuerpo denso">${conmutador}${cuerpo}</div>` : vacio);
```

`src/main.ts`, `case 'resultados'`: `renderResultados({ consulta: q, grupos: store.buscarPorTexto(q), orden })`. Escribir en la caja cambia el hash de la consulta: si eso cuenta como cambio de pantalla y resetea `orden`, está bien (es otra búsqueda); si no, también está bien. No agregar lógica para eso.

- [ ] **Step 4: Correr todo** — `npx vitest run && npm run typecheck && npm run build` → PASS.

- [ ] **Step 5: Commit local**

```bash
git add src/ tests/
git commit -m "Ordenar por duración en la búsqueda, dentro de cada grupo"
```

---

### Task 7: El skill y los documentos

**Files:**
- Modify: `skills/recetario/SKILL.md`, `product-design/plan/decision-log.md`, `product-design/plan/BACKLOG.md`, `product-design/product/specs/E02-Encontrar.md`, `E04-Corregir.md`, `E05-Cimientos.md`, `product-design/ux/design-system.md`, `CLAUDE.md`, `docs/superpowers/specs/2026-09-16-duracion-design.md`

- [ ] **Step 1: `skills/recetario/SKILL.md`** — la fila de `tiempo` de la tabla pasa a: «uno de estos cinco, tal cual: `~15 min`, `~30 min`, `~60 min`, `>60 min`, `>1 día`. Cuenta el tiempo hasta comer, con reposo y horno. Si la fuente no lo dice, no lo pongas.» Cambiar el ejemplo `tiempo: 40 min` a `tiempo: ~30 min`. La fila de la tabla de errores «Usar formatos de `tiempo` distintos en cada receta» pasa a «Escribir `tiempo` con otro formato que los cinco valores | La app lo lee como sin duración: no aparece, no filtra y no ordena.»

- [ ] **Step 2: `decision-log.md`** — una fila al final:

| 2026-09-16 | **La duración es uno de cinco valores —`~15 min`, `~30 min`, `~60 min`, `>60 min`, `>1 día`— con relojito, y las listas se filtran y ordenan por ella.** | P29: `tiempo` era texto libre y no servía para buscar algo rápido. | Minutos exactos; dos campos horas y minutos; texto libre interpretado; rangos «15–30»; una clave nueva `duracion`; un control de orden en el encabezado o en el carrusel; separadores por valor; favoritas en bloque aparte al ordenar por duración; filtro que acumula hacia abajo. | Se carga con un toque y se lee igual en el `.md` y en la app. Misma clave `tiempo`: siete claves. Lo inválido se lee como sin duración, como `dificultad`. Filtro exacto que suma, en una fila propia; orden A–Z o duración en una fila propia; en la búsqueda sólo orden. | `recipe.ts`, `catalogo.ts`, `iconos.ts`, `editor.ts`, `componentes.ts`, `fichas-receta.ts`, `categoria.ts`, `tag.ts`, `resultados.ts`, `main.ts`. |

(Ajustar a las columnas reales de la tabla del decision-log.)

- [ ] **Step 3: `BACKLOG.md`** — P29 **Resuelto** `[2026-09-16]` con un resumen y el spec. En §2, «Ordenar dentro de una categoría» y «Un control de filtro visible sobre las listas»: nota fechada de que la duración los resolvió para ese criterio.

- [ ] **Step 4: specs de producto** — `E05-Cimientos.md`: donde se describe `tiempo`, los cinco valores y la regla de lo inválido `[cambio del 2026-09-16]`. `E04-Corregir.md`: el campo «Duración» del editor. `E02-Encontrar.md`: el filtro y el orden por duración, y en qué pantallas.

- [ ] **Step 5: `design-system.md`** — §3.4 suma los cinco relojitos; y los botones de duración del editor, la fila de chips de duración y el conmutador de orden, con los valores de `base.css`.

- [ ] **Step 6: `CLAUDE.md`** — bloque «Hecho el 2026-09-16 — la duración (P29)» con spec y plan, y que queda probarlo en el teléfono (§11 del spec); «Para retomar» lo nombra; la fila «Ordenar el home por cantidad de recetas» suma que las listas se pueden ordenar por duración; la lista de pendientes saca P29.

- [ ] **Step 7: el spec** — «Estado» pasa a «Implementado».

- [ ] **Step 8: Commit local**

```bash
git add skills product-design CLAUDE.md docs/superpowers/specs
git commit -m "El skill y los documentos dicen cómo es la duración"
```

---

## Verificación final, en el teléfono

La del §11 del spec.
