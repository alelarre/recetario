# Backlog

Sólo pendientes. El **estado** dice en qué anda cada uno:

- `Abierto` — nadie lo está tocando.
- `Implementando` — hay una sesión trabajándolo: no lo tomes.
- `Falta probar` — hecho, esperando la prueba en el teléfono.

Una entrada se borra de la tabla recién cuando está terminada y lista para
probar; una que quedó a medias vuelve a `Abierto`.

| ID | Descripción | Detalle | Estado |
|---|---|---|---|
| P14 | Rehacer el skill del agente (`skills/recetario/`) | Quedó en un esquema viejo: escribe la clave `completa` en vez del tag `incompleta`, tiene su propia lista de tags reservados —le falta `menú diario`—, no conoce los cinco valores de `tiempo` y tampoco el depósito de fotos: la sección `## Fotos`, las referencias `![](foto:N)` y `foto: foto:N` como cabecera. El skill nuevo no repite reglas que ya están en `src/`. Tampoco escribe la fila del índice: después de cargar recetas directo a Drive hay que tocar *Ajustes → Reindexar*. Para una receta suelta alcanza con que el skill devuelva el `.md` y la app lo reciba compartido o pegado: la app lo guarda con su fila. Lo que sigue sin resolver es la carga en masa, porque el conector de Google Drive de claude.ai no escribe planillas ni reescribe archivos. | Abierto |
| P60 | Un selector que ningún test alcanza | `revisarCategoria` (`src/main.ts:1782`) busca `'#app [data-formulario]'`, y ese selector nunca encuentra nada en el DOM falso de los tests: el doble responde a `[data-formulario]` a secas. No es un error de la app —en el navegador el formulario está adentro de `#app`—, pero significa que ese camino corre sin cobertura. Visto revisando P59. Sale con que el doble entienda el selector con prefijo, o con que la app use el mismo que el resto. | Abierto |
| P36 | Probar el velo durante las escrituras | Checklist en `docs/superpowers/specs/2026-09-17-bloqueo-escrituras-design.md` §7: guardar una receta, descartar un borrador, borrar una categoría, sin red, y favorito sin velo. Se suma el dibujo nuevo: la olla que se revuelve y el cierre con el tilde al guardar una receta, que tiene que dibujarse sin trabar el cierre del editor. Al terminar se borra el spec. | Falta probar |
| P26 | Probar la carpeta sin explorador | Checklist en `docs/superpowers/specs/2026-09-18-carpeta-sin-explorador-design.md` §9: crear «Recetario», cambiar de carpeta con el Picker y cancelarlo. Al terminar se borran el spec y su plan. | Falta probar |
| P32 | Probar el plan de la semana | Checklist en `docs/superpowers/specs/2026-09-17-planificador-design.md` §10: agregar, sacar, reiniciar, la lista de compras por WhatsApp, y que arranque en el día de hoy. Al terminar se borran el spec y su plan. | Falta probar |
| P37 | Probar que «Salir» cierra la sesión | Salir revoca el token y recarga: después no tiene que verse nada del Recetario ni reconectarse solo. | Falta probar |
| P38 | Probar la captura nueva | Reinstalar la PWA —el manifest cambió— y compartir un reel de Instagram (fuente el link, el texto en la nota), una página desde Chrome (fuente la URL, sin el título), un texto sin link desde WhatsApp (sin fuente, todo en la nota), y guardar sin título («Borrador dd/mm hh:mm»). | Falta probar |
| P40 | Probar las fotos en los borradores | Checklist en `docs/superpowers/specs/2026-09-19-fotos-en-borradores-design.md` §13. Hay que reinstalar la PWA. Al terminar se borran el spec y su plan. | Falta probar |
| P41 | Probar las fotos de las recetas y lo que salió del backlog | Checklist en `docs/superpowers/specs/2026-09-19-fotos-de-recetas-design.md` §14: agregar fotos y guardarlas, poner una en una línea desde el botón del cursor, *Portada*, *Sacar*, salir sin guardar, el libro del velo, el visor, convertir un borrador con fotos, el PDF, el link del invitado, una categoría con foto propia, una `foto` con una URL externa rota —el bloque no se dibuja—, abrir una receta ya vista en modo avión, y *Borrar datos locales*. Suma lo que salió del backlog en la misma tanda: el velo que aparece al tocar Guardar y frena el scroll, la olla que revuelve y el tilde al terminar, el carrusel de fotos en la primera ficha, el botón de foto en la línea del cursor —con el teclado abierto y con el campo scrolleado—, «Portada» en el editor, los botones Cámara y Galería, y un ingrediente con foto cuyo id tenga guión. Al terminar se borran el spec y su plan. | Falta probar |
| P61 | Simplificar el flujo de creación de recetas | Nuevo y borrador se solapan en funcionalidad. Revisar el flujo entero y simplificarlo. | Abierto |
| P68 | Revisar si el token se invalida de más | `auth.token()` (`src/auth.ts:95`) sólo vuelve a pedir (`pedir('')`) si `Date.now() >= vence`, y `vence` sale de `expires_in - 60s` (`auth.ts:81`) guardado en `localStorage` (`auth.ts:29-31`, clave `recetario-auth`): un token vigente no debería repedirse hasta pasada casi una hora, y `arrancar()` (`main.ts:717-724`) lo llama una sola vez por apertura. La hipótesis más fuerte: `pedir('')` no comparte una renovación en curso entre llamadas concurrentes, y el reindexado lee hasta `TOPE_LECTURAS = 6` (`store.ts:34`) archivos en paralelo —si el token vence justo ahí, las hasta 6 llamadas a `auth.token()` ven `Date.now() >= vence` antes de que ninguna actualice el estado, y cada una abre su propio popup de Google (`pedir` siempre pasa por popup, incluso en la renovación silenciosa: `auth.ts:13-17`), en vez de uno solo compartido. **Cómo probarlo:** en el navegador, abrir DevTools → Application → Local Storage, editar la clave `recetario-auth` poniéndole `vence` en `0`, recargar la página (así el valor pasado queda en memoria) y disparar en el momento *Ajustes → Reindexar*; si se ven varios popups de Google abrirse y cerrarse casi juntos —o la pestaña Network muestra varios pedidos simultáneos al endpoint de token de Google— confirma la hipótesis. La salida sería que las llamadas concurrentes a `token()` esperen una única promesa de renovación en curso en vez de disparar cada una la suya. | Abierto |
| P69 | Menú semanal: ofrecer el resto de las categorías | Sumar debajo de «Menú diario» (`bloqueDeAgregar`, `src/ui/plan-agregar.ts:45-62`) un bloque «Categorías» con las 16, siempre —no sólo sin escribir nada—, en el mismo orden alfabético que el Recetario (`ui/recetario.ts:32-33`). Tocar una filtra esta misma pantalla a las recetas de esa categoría, sin navegar a `#/c/<categoria>`, con la acción de siempre (`tarjeta(..., { accion: ELEGIR })`, la misma que usa Menú diario). Con `accion` la tarjeta ya es un botón en vez de un link, pero se ve igual que una que navega (`componentes.ts:127-157`): sumar un botón «+» a la derecha que marque que tocarla agrega en vez de abrir la receta —vale para las tres listas que usan `accion` en esta pantalla: Menú diario, los resultados de búsqueda y el bloque nuevo de categorías—. Necesita también: la categoría elegida como estado de la pantalla (como `consulta` hoy), un tile que dispare una acción en vez de un `href` (el `tile()` de `componentes.ts` hoy sólo linkea), y volver al bloque de Menú diario al sacar la selección. | Abierto |
| P70 | Limpiar comentarios que referencian el backlog | Hay comentarios en el código que citan entradas del backlog o decisiones puntuales. Limitarlos a lo mínimo que explique qué hace el código. | Abierto |
| P71 | Limpieza general de documentos del proyecto | Revisar y poner en orden los documentos del proyecto. | Abierto |
| P72 | Borrar los artefactos de claude.ai | Sacar los Artifacts publicados en claude.ai que quedaron del desarrollo. | Abierto |
| P73 | Actualizar la documentación de producto | Poner `product-design/` al día con el estado actual del producto. | Abierto |
| P74 | Revisar cómo están construidos los tests | Repasar la estructura y los patrones de los tests actuales. | Abierto |
| P75 | El selector de carpeta dice que se puede crear, pero no deja | En Ajustes, el texto del selector de carpeta dice «podrás crearla», pero el selector sólo permite elegir una carpeta existente, no crearla. | Abierto |
| P76 | El indexado inicial no da feedback del progreso | La primera vez que se abre sesión, sin índice todavía, el indexado inicial sólo muestra un spinner y parece colgado —aunque termina y lleva a la pantalla principal. Agregar mensajes de estado a medida que avanzan los pasos del proceso. | Abierto |
| P77 | Botones subrayados en Ajustes → Categorías | En Ajustes, algunos botones aparecen subrayados: «Categorías» (que lleva a la lista) y, adentro, «+ Nueva». Queda raro. | Abierto |
