# Backlog

Sólo pendientes abiertos. Lo que se resuelve se borra de la tabla.

| ID | Descripción | Detalle | Estado |
|---|---|---|---|
| P14 | Rehacer el skill del agente (`skills/recetario/`) | Quedó en un esquema viejo: escribe la clave `completa` en vez del tag `incompleta`, tiene su propia lista de tags reservados —le falta `menú diario`— y no conoce los cinco valores de `tiempo`. El skill nuevo no repite reglas que ya están en `src/`. Tampoco escribe la fila del índice: después de cargar recetas directo a Drive hay que tocar *Ajustes → Reindexar*. El conector de Google Drive de claude.ai no escribe planillas ni reescribe archivos, así que no puede correr `src/compartido.ts`; para una receta suelta alcanza con que el skill devuelva el `.md` y la app lo reciba compartido o pegado. Sigue sin resolver la carga en masa. | Abierto |
| P19-3b | Imágenes propias de categorías | La foto de una categoría puede ser un archivo del usuario en Drive, además del catálogo de `src/categorias/`. La carpeta guarda el id del archivo en sus `appProperties`. La imagen se pide con el token una sola vez y se guarda en Cache Storage por id de archivo; reindexar vacía ese cache y *Borrar datos locales* también lo borra. | Abierto |
| P26 | Rediseñar el selector de carpetas | El actual (`src/ui/carpeta.ts`, `#/carpeta`) no convence: una lista de carpetas propias por nivel, con «Encontradas» arriba, «Usar esta carpeta», «Crear una carpeta nueva acá» y una confirmación antes del setup. Primero entender qué no convence y presentar propuestas. | Abierto |
| P27 | Probar los tags especiales en el teléfono | Las checklists están en `docs/superpowers/specs/2026-09-16-tags-especiales-design.md` §11 y `2026-09-16-tags-especiales-2-design.md` §10. Al terminar se borran los dos specs. | Falta probar |
| P28 | Probar «Convertir con Claude» en el teléfono y en la Mac | La checklist está en `docs/superpowers/specs/2026-09-16-convertir-con-claude-design.md` §6. Al terminar se borra el spec. | Falta probar |
| P29 | Probar la duración en el teléfono | La checklist está en `docs/superpowers/specs/2026-09-16-duracion-design.md` §11. Al terminar se borra el spec. | Falta probar |
| P30 | Reindexar lee los `.md` de a uno | Un `drive.leerTexto()` por archivo, sin paralelismo ni loteo: ~40 segundos con ~60 recetas, minutos con las mil del objetivo. Se paga en cada *Reindexar* y cada vez que sube `SCHEMA_VERSION`. Conviene resolverlo antes de migrar contenido en masa. | Abierto |
| P31 | Migrar el contenido que falta | Ya están ~60 recetas del recetario original y del PDF de pescados. Faltan los documentos temáticos (fondues, pan, macarons, fermentación) y el Doc de ~7,3 MB. Depende de P14, y conviene P30 antes. | Abierto |
| P32 | El planificador y la lista de compras | Diseñado y afuera a propósito: es el único job hipotético del producto. El spec está en `product-design/product/specs/E06-Planificar.md` y los mockups en `product-design/ux/mockups/` (12 y 13). Su entrada iría en el Recetario, debajo de las categorías. | Sin fecha |
| P33 | Limpieza de código y comentarios legacy | Recorrer `src/` y `tests/` y sacar lo que quedó de etapas anteriores. **Comentarios:** los que cuentan historia en vez de decir qué hace el código —fechas, «antes era X», la cronología de `SCHEMA_VERSION` en `config.ts`, las cabeceras de `tokens.css` y `base.css` sobre los mockups—; los que citan documentos que ya no existen (`§4.4 del spec`, el decision log, specs y planes de `docs/superpowers/`); los ids P de pendientes ya cerrados; y las capacidades con numeración vieja (`main.ts` cita C05.9.1 y C05.9.2, que en el spec son C05.7.1 y C05.7.2). **Código sin uso:** `ICO.puntos`, la clase `.compacta` de `base.css`, `--txt-titulo-s` y `.check` de `tokens.css` —mirar antes el `<button class="check">` que arma `markdown.ts` en los pasos de cocina—, y lo que aparezca al buscar exports, clases CSS y helpers de test sin referencias. Sin cambios de comportamiento: tests, typecheck y build en verde antes y después. | Abierto |
| P34 | Contradicciones entre los specs y el código | Salieron al poner al día `product-design/`; cada una pide decidir si se arregla el código o se cambia el spec. (1) C02.6.3: tocar un tag en la receta debería llevar a la lista filtrada, y el código sólo redibuja la receta. (2) C01.8.2: con la sesión vencida la captura debería ofrecer reconectar, y muestra «No se pudo guardar. Revisá la conexión.» para cualquier falla. (3) R1 de E05: ningún error muestra el mensaje crudo de Google, pero el arranque pinta `No pude arrancar: <mensaje crudo>`. (4) C02.1.2: el Recetario sin recetas debería explicar por dónde entran, y no dibuja esa línea. (5) C05.1.2: dice que el orden de las secciones del `.md` se respeta al reescribir, y `serialize` usa un orden fijo. (6) Los mensajes de error mezclan «No se pudo…» y «No pude…», y varios estados vacíos sugieren qué hacer aunque `brand-identity.md` §3.2 pide que no. | Abierto |
