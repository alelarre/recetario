# Backlog

Sólo pendientes. El **estado** dice en qué anda cada uno:

- `Abierto` — nadie lo está tocando.
- `Implementando` — hay una sesión trabajándolo: no lo tomes.
- `Falta probar` — hecho, esperando la prueba en el teléfono.

Una entrada se borra de la tabla recién cuando está terminada y lista para
probar; una que quedó a medias vuelve a `Abierto`.

| ID | Descripción | Detalle | Estado |
|---|---|---|---|
| P14 | Rehacer el skill del agente (`skills/recetario/`) | Quedó en un esquema viejo: escribe la clave `completa` en vez del tag `incompleta`, tiene su propia lista de tags reservados —le falta `menú diario`—, no conoce los cinco valores de `tiempo` y tampoco el depósito de fotos: la sección `## Fotos`, las referencias `![](foto:N)` y `foto: foto:N` como cabecera. El skill nuevo no repite reglas que ya están en `src/`. Tampoco escribe la fila del índice: después de cargar recetas directo a Drive hay que tocar *Ajustes → Reindexar*. Para una receta suelta alcanza con que el skill devuelva el `.md` y la app lo reciba compartido o pegado: la app lo guarda con su fila. Lo que sigue sin resolver es la carga en masa, porque el conector de Google Drive de claude.ai no escribe planillas ni reescribe archivos. | Abierto |
| P54 | Cada foto sabe si se usa, y no se muestra dos veces | Hoy una foto puede aparecer tres veces: de portada, adentro de un paso y otra vez en el carrusel. Cada foto del depósito pasa a tener un **uso, que se calcula y no se guarda**: *portada* (la nombra `foto:`), *usada* (la nombra un `![](foto:N)` de alguna sección) —las dos no se excluyen— y *sin uso* (ninguna de las dos). El cálculo es una función pura en `src/fotos-receta.ts`, sobre la receta ya parseada. **En el editor**, la fila de fotos muestra de qué tipo es cada una, con íconos, subsecciones o color —no con letras—, para saber qué falta ubicar. **En la receta**, el carrusel de la primera ficha muestra sólo las sin uso: la portada ya está arriba y las usadas están en su línea. **En el PDF**, la galería del final lleva sólo las sin uso, por lo mismo. Queda por resolver qué recorre el visor cuando se abre desde el carrusel —sólo las que se ven, o todo el depósito— y qué pasa con un carrusel que queda vacío porque todas están ubicadas. Conviene mirarlo con un mock antes de codear. | Abierto |
| P55 | Las pantallas del menú lateral muestran la hamburguesa, no el volver | El menú lateral llega al Recetario, a Borradores, al plan de la semana y a Ajustes, pero el encabezado sólo dibuja la hamburguesa en algunas: *Nueva receta* y el *plan de la semana* muestran el volver. Las que están en el menú tienen que abrirlo desde su encabezado; el volver queda para las que se entra desde otra pantalla. `PANTALLAS_CON_MENU` (`src/main.ts:2592`) ya lista cuáles son —el plan está ahí, así que el gesto sí lo abre pero el botón no—; revisar qué hace cada pantalla con `encabezado({ izquierda })` y si *Nueva receta* tiene que estar en esa lista. | Abierto |
| P56 | La lista de compras se estira a lo ancho en vez de cortar | En el plan de la semana, una línea larga de la lista de compras no corta en varios renglones: se extiende y aparece un scroll horizontal. Es un `white-space`, un `min-width: 0` o una palabra sin espacios que no rompe; mirar `.compras` en el CSS y el ancho de la fila. Se ve en el teléfono con un ingrediente de nombre largo. | Abierto |
| P57 | Con el menú lateral abierto, la página de atrás se desplaza | En Android, con el menú desplegado, un deslizamiento vertical scrollea la pantalla que está debajo del velo, que es justamente lo que el velo tapa. Las fichas al pie y el velo de escritura ya lo frenan con `html:has(...) { overflow: hidden }` (`src/ui/base.css`): al menú lateral le falta la misma regla, o el equivalente para `.velo-lat.on`. | Abierto |
| P58 | La ficha de compartir queda desalineada en Android | Visto probando P32, al compartir la lista de compras: la ficha que se abre al pie no queda alineada con el resto de la pantalla y aparece scroll vertical **y horizontal**. La ficha es `.hoja-compartir` (`src/ui/base.css`), `position: fixed` con `max-width: 680px` y `margin: 0 auto`; el cuadro del texto usa `word-break: break-all` y `max-height: 40vh`. Sospechas a mirar en el teléfono: el ancho contra la barra del navegador —`100vw` contra el ancho real—, el `env(safe-area-inset-bottom)`, y si algo adentro fuerza un ancho mayor que el de la ficha. Se ve en el plan de la semana y hay que comprobar si también en la receta. | Abierto |
| P59 | El borrador también agrega fotos por URL | El editor de recetas suma fotos de tres formas —cámara, galería y por URL, que la baja y la guarda en Drive—; el borrador y la captura siguen con dos. Tiene que ser igual en los tres: mismo control, misma ficha, mismas salidas —lo que no se puede bajar queda como link, `https://` obligatorio, corte a los 20 s—. El borrador escribe su `.md` en el momento, así que la foto bajada se sube ahí mismo y no al guardar; y sigue valiendo el tope de cinco. Visto probando P40. | Abierto |
| P36 | Probar el velo durante las escrituras | Checklist en `docs/superpowers/specs/2026-09-17-bloqueo-escrituras-design.md` §7: guardar una receta, descartar un borrador, borrar una categoría, sin red, y favorito sin velo. Se suma el dibujo nuevo: la olla que se revuelve y el cierre con el tilde al guardar una receta, que tiene que dibujarse sin trabar el cierre del editor. Al terminar se borra el spec. | Falta probar |
| P26 | Probar la carpeta sin explorador | Checklist en `docs/superpowers/specs/2026-09-18-carpeta-sin-explorador-design.md` §9: crear «Recetario», cambiar de carpeta con el Picker y cancelarlo. Al terminar se borran el spec y su plan. | Falta probar |
| P32 | Probar el plan de la semana | Checklist en `docs/superpowers/specs/2026-09-17-planificador-design.md` §10: agregar, sacar, reiniciar, la lista de compras por WhatsApp, y que arranque en el día de hoy. Al terminar se borran el spec y su plan. | Falta probar |
| P37 | Probar que «Salir» cierra la sesión | Salir revoca el token y recarga: después no tiene que verse nada del Recetario ni reconectarse solo. | Falta probar |
| P38 | Probar la captura nueva | Reinstalar la PWA —el manifest cambió— y compartir un reel de Instagram (fuente el link, el texto en la nota), una página desde Chrome (fuente la URL, sin el título), un texto sin link desde WhatsApp (sin fuente, todo en la nota), y guardar sin título («Borrador dd/mm hh:mm»). | Falta probar |
| P40 | Probar las fotos en los borradores | Checklist en `docs/superpowers/specs/2026-09-19-fotos-en-borradores-design.md` §13. Hay que reinstalar la PWA. Al terminar se borran el spec y su plan. | Falta probar |
| P41 | Probar las fotos de las recetas y lo que salió del backlog | Checklist en `docs/superpowers/specs/2026-09-19-fotos-de-recetas-design.md` §14: agregar fotos y guardarlas, poner una en una línea desde el botón del cursor, *Portada*, *Sacar*, salir sin guardar, el libro del velo, el visor, convertir un borrador con fotos, el PDF, el link del invitado, una categoría con foto propia, una `foto` con una URL externa rota —el bloque no se dibuja—, abrir una receta ya vista en modo avión, y *Borrar datos locales*. Suma lo que salió del backlog en la misma tanda: el velo que aparece al tocar Guardar y frena el scroll, la olla que revuelve y el tilde al terminar, el carrusel de fotos en la primera ficha, el botón de foto en la línea del cursor —con el teclado abierto y con el campo scrolleado—, «Portada» en el editor, los botones Cámara y Galería, y un ingrediente con foto cuyo id tenga guión. Al terminar se borran el spec y su plan. | Falta probar |
