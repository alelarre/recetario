# Backlog

Sólo pendientes. El **estado** dice en qué anda cada uno:

- `Abierto` — nadie lo está tocando.
- `Implementando` — hay una sesión trabajándolo: no lo tomes.
- `Falta probar` — hecho, esperando la prueba en el teléfono.

Una entrada se borra de la tabla recién cuando está terminada y lista para
probar; una que quedó a medias vuelve a `Abierto`.

| ID | Descripción | Detalle | Estado |
|---|---|---|---|
| P14 | Rehacer el skill del agente (`skills/recetario/`) | Quedó en un esquema viejo: escribe la clave `completa` en vez del tag `incompleta`, tiene su propia lista de tags reservados —le falta `menú diario`—, no conoce los cinco valores de `tiempo` y tampoco el depósito de fotos: la sección `## Fotos`, las referencias `![](foto:N)` y `foto: foto:N` como cabecera. El skill nuevo no repite reglas que ya están en `src/`. Tampoco escribe la fila del índice: después de cargar recetas directo a Drive hay que tocar *Ajustes → Reindexar*. Para una receta suelta alcanza con que el skill devuelva el `.md` y la app lo reciba compartido o pegado: la app lo guarda con su fila. Lo que sigue sin resolver es la carga en masa, porque el conector de Google Drive de claude.ai no escribe planillas ni reescribe archivos. | Abierto |
| P47 | El velo tiene que aparecer al tocar Guardar | Hecho en la rama `backlog-fotos`: el velo se muestra desde el toque en Guardar, la captura y agregar al plan, y la página de atrás no scrollea mientras dura. Lo que tarda no era código: el `.md` de base se lee de Drive en 260–290 ms. Sale de la tabla cuando la rama llegue a `main` y se pueda probar. | Implementando |
| P43 | Iterar la animación de guardar | Hecho en la rama `backlog-fotos`: el velo pasa a ser una olla que se revuelve mientras guarda, y cuando la escritura sale bien baja la tapa y aparece un tilde verde, en 500 ms, sin bloquear la pantalla. Por ahora el cierre lo usa sólo el Guardar del editor. Sale de la tabla cuando la rama llegue a `main` y se pueda probar. | Implementando |
| P53 | El carrusel de fotos vuelve al principio al cerrar el visor | Cada redibujado reescribe la receta entera, así que la pista del carrusel pierde su `scrollLeft`: mirar las fotos, abrir la quinta y volver obliga a deslizar de nuevo. Al carrusel de tags le pasa lo mismo, pero ahí no hay visor del que volver. La salida es recordar el `scrollLeft` de la pantalla y reponerlo después de pintar. Decidir con el teléfono en la mano si molesta de verdad. | Abierto |
| P52 | El velo parpadea al agregar varias fotos | Al elegir varias fotos de una, el velo se apaga y se prende entre una y otra: cada foto achica y sube por separado, y cada paso es su propia escritura. Tendría que quedar tapado de punta a punta. Visto al hacer P47, que no lo cubría. | Abierto |
| P44 | Las fotos de la receta, en un carrusel en la primera ficha | Hecho en la rama `backlog-fotos`: la ficha *Fotos* del final se fue y el depósito entero va en un carrusel dentro de la primera ficha, debajo de la descripción y arriba del divisor de la fuente, con fotos de 132 px —entran dos y media en el teléfono—. El marco salió a `carrusel()` (`src/ui/componentes.ts`), que usan los tags y las fotos, y su CSS pasó de `base.css` a `tokens.css` como componente del sistema, con el color del degradé por variable. La flecha ahora mueve la pista de su propio marco (`desplazarCarrusel`, `src/ui/pintar.ts`), así que conviven dos carruseles en una pantalla; el invitado sumó las dos flechas a su lista cerrada de acciones. El PDF no cambió. Sale de la tabla cuando la rama llegue a `main` y se pueda probar. | Implementando |
| P48 | En Editar, el primer «Foto» se llama «Portada» | Hecho en la rama `backlog-fotos`: el campo de la cabecera se llama **Portada**, como la acción de la ficha de una foto, y *Fotos* queda sólo para el depósito. `E04-Corregir.md` C04.2.1 y C04.2.1d. Sale de la tabla cuando la rama llegue a `main` y se pueda probar. | Implementando |
| P50 | Sacar una foto con la cámara al agregar fotos | *Agregar foto* (`filaDeFotos`, `src/ui/componentes.ts`) abre `<input type="file" accept="image/*" multiple>`: según el teléfono, el sistema ofrece la cámara o abre directo el explorador de archivos. Tiene que poder sacarse la foto en el momento. `capture` en el input lleva directo a la cámara pero saca la galería, así que la salida es ofrecer las dos: *Cámara* y *Galería*, o el mismo botón con las dos opciones. Vale para el editor de recetas, la captura y el borrador. Tocar `E01` C01.2.4 y C01.6.4, y `E04` C04.3d.1. | Abierto |
| P46 | Poner una foto desde el paso, no desde la foto | Hecho en la rama `backlog-fotos`: se dio vuelta *Poner en…*, que salió con su ficha. Con el foco en un campo de contenido aparece un botón sin texto, con el ícono de foto, a la altura de la línea del cursor y pegado al borde derecho del campo; tocarlo abre la ficha con la galería del depósito y la elegida se escribe al final de esa línea. Desde ahí no se agregan fotos: eso sigue siendo la ficha *Fotos*. Sin depósito el botón no se dibuja. La altura del renglón sale de un **espejo** del campo —un calco invisible al que se le escribe el texto hasta el cursor—, porque un `textarea` no dice en qué renglón está el cursor y contar por alto de línea falla con el ajuste de línea. `lineaDelCursor` (`src/fotos-receta.ts`) es la decisión pura; `lineasDeLaReceta` y el tipo `Lugar` se fueron con la ficha vieja. `E04-Corregir.md` C04.3d.2 y C04.3d.3, `design-system.md` §6.9b. Sale de la tabla cuando la rama llegue a `main` y se pueda probar. | Implementando |
| P36 | Probar el velo durante las escrituras | Checklist en `docs/superpowers/specs/2026-09-17-bloqueo-escrituras-design.md` §7: guardar una receta, descartar un borrador, borrar una categoría, sin red, y favorito sin velo. Se suma el dibujo nuevo: la olla que se revuelve y el cierre con el tilde al guardar una receta, que tiene que dibujarse sin trabar el cierre del editor. Al terminar se borra el spec. | Falta probar |
| P26 | Probar la carpeta sin explorador | Checklist en `docs/superpowers/specs/2026-09-18-carpeta-sin-explorador-design.md` §9: crear «Recetario», cambiar de carpeta con el Picker y cancelarlo. Al terminar se borran el spec y su plan. | Falta probar |
| P32 | Probar el plan de la semana | Checklist en `docs/superpowers/specs/2026-09-17-planificador-design.md` §10: agregar, sacar, reiniciar, la lista de compras por WhatsApp, y que arranque en el día de hoy. Al terminar se borran el spec y su plan. | Falta probar |
| P37 | Probar que «Salir» cierra la sesión | Salir revoca el token y recarga: después no tiene que verse nada del Recetario ni reconectarse solo. | Falta probar |
| P38 | Probar la captura nueva | Reinstalar la PWA —el manifest cambió— y compartir un reel de Instagram (fuente el link, el texto en la nota), una página desde Chrome (fuente la URL, sin el título), un texto sin link desde WhatsApp (sin fuente, todo en la nota), y guardar sin título («Borrador dd/mm hh:mm»). | Falta probar |
| P40 | Probar las fotos en los borradores | Checklist en `docs/superpowers/specs/2026-09-19-fotos-en-borradores-design.md` §13. Hay que reinstalar la PWA. Al terminar se borran el spec y su plan. | Falta probar |
| P41 | Probar las fotos de las recetas | Checklist en `docs/superpowers/specs/2026-09-19-fotos-de-recetas-design.md` §14: agregar fotos y guardarlas, *Poner en…*, *Portada*, *Sacar*, salir sin guardar, el libro del velo, el visor, convertir un borrador con fotos, el PDF, el link del invitado, una categoría con foto propia, una `foto` con una URL externa rota —el bloque no se dibuja—, abrir una receta ya vista en modo avión, y *Borrar datos locales*. Al terminar se borran el spec y su plan. | Falta probar |
