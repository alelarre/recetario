# Backlog

Sólo pendientes. El **estado** dice en qué anda cada uno:

- `Abierto` — nadie lo está tocando.
- `Implementando` — hay una sesión trabajándolo: no lo tomes.
- `Falta probar` — hecho, esperando la prueba en el teléfono.
- `En espera` — decidido que no se toca hasta que se resuelva otra entrada.

Una entrada se borra de la tabla recién cuando está terminada y lista para
probar; una que quedó a medias vuelve a `Abierto`.

| ID | Descripción | Detalle | Estado |
|---|---|---|---|
| P14 | Rehacer el skill del agente (`skills/recetario/`) | Quedó en un esquema viejo: escribe la clave `completa` en vez del tag `borrador`, tiene su propia lista de tags reservados —le falta `menú diario`—, no conoce los cinco valores de `tiempo` y tampoco el depósito de fotos: la sección `## Fotos`, las referencias `![](foto:N)` y `foto: foto:N` como cabecera. El skill nuevo no repite reglas que ya están en `src/`. Tampoco escribe la fila del índice: después de cargar recetas directo a Drive hay que tocar *Ajustes → Reindexar*. Para una receta suelta alcanza con que el skill devuelva el `.md` y la app lo reciba compartido o pegado: la app lo guarda con su fila. Lo que sigue sin resolver es la carga en masa, porque el conector de Google Drive de claude.ai no escribe planillas ni reescribe archivos. Tiene que usar el tag `borrador` y, cuando el pedido trae un `id: <id>`, devolverlo como última línea del frontmatter: con él, la receta compartida abre el editor de la receta que la pidió. | Abierto |
| P86 | Probar en el teléfono la arquitectura frontend | Lo que ningún test cubre de la navegación, el velo y los componentes nuevos: el atrás de Android cerrando el menú, el visor, las fichas —compartir y las de fotos del editor— y la categoría elegida en *Agregar al plan*, sin salir de la pantalla; las salidas: borrar una receta (vuelve a la lista de donde se venía, también desde Borradores), *Salir* de la cocina (vuelve a donde se eligió la receta), guardar una receta nueva (queda en la receta creada) y elegir en *Agregar al plan* (vuelve al plan con una sola entrada); el velo con la olla y el tilde en cada escritura —receta, categoría, plan, carpeta— y ningún botón que diga «Guardando…»; el reindexado de *Ajustes* bloqueando la pantalla, con su tarjeta y su barra; el menú fijo desde 900 px en la receta y el editor; el aviso de foto ausente en la muestra de la categoría; los espacios entre filas y grupos en las listas y en *Agregar al plan*, la primera ficha de una receta sin fuente y el carrusel de duraciones. | Falta probar |
| P87 | Modo cocina: la foto de un ingrediente abre un visor invisible | Tocar la foto de un ingrediente en el modo cocina abre un visor que no se dibuja y deja una capa en el historial: el próximo atrás no hace nada visible. | Abierto |
| P88 | Editar: vaciar el título guarda el anterior sin avisar | Al editar una receta existente, si se vacía el título se guarda el que tenía, sin aviso. Tiene que avisar «Ponele un título antes de guardar.» como en una receta nueva sin `borrador`. | Abierto |
| P89 | Borradores con un tag activo y sin resultados dice «No hay borradores.» | Con un tag encendido que no deja nada, la pantalla dice que no hay borradores aunque haya. Tiene que decir lo mismo que una lista filtrada sin resultados. | Abierto |
| P91 | La pantalla de la carpeta base habla en primera persona | «Voy a usar X. Si faltan categorías, las creo, y después indexo…» (`src/ui/carpeta.ts`) va contra la voz de la app (brand-identity §3). Pasarla a impersonal. | Abierto |
| P92 | El plan tacha en rojo una receta que ya no está | La línea del plan de una receta borrada usa `--error` en el borde (`base.css`, `.celda .it.ida`), contra la regla de que el error no marca un estado del contenido (design-system §2.2, §8). Usar `--fg-3`; el tachado ya lo dice. | Abierto |
| P93 | Los ingredientes de las variaciones no entran en la búsqueda | E03 y la IA dicen que la búsqueda por ingrediente incluye los de las variaciones; hoy no. | Abierto |
| P94 | El plan muestra el título viejo de una receta renombrada | Cada línea del plan muestra el título escrito en `_plan.md`. Mostrar el del índice, que ya está en memoria; si hacerlo implica leer el índice de más, no se hace. | Abierto |
| P95 | Una receta que ya no está en Drive muestra «No se pudo leer» con Reintentar | Reintentar no sirve si el archivo no existe. Tiene que decir que la receta ya no está y ofrecer volver. | Abierto |
| P96 | La ayuda de la ficha Fotos es prosa en tamaño micro | El párrafo de ayuda de la ficha Fotos del editor va en `--txt-micro`, y micro es «nunca prosa» (design-system §3.2). Subirlo a chico. | Abierto |
| P100 | Probar en el teléfono los arreglos de listas y del editor | Los chips de tags cuentan lo mismo que la lista que abren: sin borradores en Inicio, la categoría y la lista por tag, y en Borradores sólo los borradores.<br>Al pie del editor, *Convertir con Agente*, *Guardar* y *Borrar receta* con el mismo aire entre los tres.<br>Tocar *Borrar receta* —y *Borrar categoría*— trae la confirmación entera a la vista, sin quedar debajo del encabezado, con el foco en *Cancelar*.<br>Abrir una receta desde el fondo de Borradores o de una lista: la lista no salta ni pierde las fotos mientras se lee, y la receta abre desde arriba. | Falta probar |
