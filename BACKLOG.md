# Backlog

Sólo pendientes. El **estado** dice en qué anda cada uno:

- `Abierto` — nadie lo está tocando.
- `Implementando` — hay una sesión trabajándolo: no lo tomes.
- `Falta probar` — hecho, esperando la prueba en el teléfono.
- `En espera` — decidido que no se toca hasta que se resuelva otra entrada.

Una entrada se borra de la tabla recién cuando está terminada y lista para
probar; una que quedó a medias vuelve a `Abierto`.

| ID | Descripción | Detalle | Estado |
|---|---|---|---|
| P86 | Probar en el teléfono la arquitectura frontend | Lo que ningún test cubre de la navegación, el velo y los componentes nuevos: el atrás de Android cerrando el menú, el visor, las fichas —compartir y las de fotos del editor— y la categoría elegida en *Agregar al plan*, sin salir de la pantalla; las salidas: borrar una receta (vuelve a la lista de donde se venía, también desde Borradores), *Salir* de la cocina (vuelve a donde se eligió la receta), guardar una receta nueva (queda en la receta creada) y elegir en *Agregar al plan* (vuelve al plan con una sola entrada); el velo con la olla y el tilde en cada escritura —receta, categoría, plan, carpeta— y ningún botón que diga «Guardando…»; el reindexado de *Ajustes* bloqueando la pantalla, con su tarjeta y su barra; el menú fijo desde 900 px en la receta y el editor; el aviso de foto ausente en la muestra de la categoría; los espacios entre filas y grupos en las listas y en *Agregar al plan*, la primera ficha de una receta sin fuente y el carrusel de duraciones. | Falta probar |
| P100 | Probar en el teléfono los arreglos de listas y del editor | Los chips de tags cuentan lo mismo que la lista que abren: sin borradores en Inicio, la categoría y la lista por tag, y en Borradores sólo los borradores.<br>Al pie del editor, *Convertir con Agente*, *Guardar* y *Borrar receta* con el mismo aire entre los tres.<br>Tocar *Borrar receta* —y *Borrar categoría*— trae la confirmación entera a la vista, sin quedar debajo del encabezado, con el foco en *Cancelar*.<br>Abrir una receta desde el fondo de Borradores o de una lista: la lista no salta ni pierde las fotos mientras se lee, y la receta abre desde arriba. | Falta probar |
| P102 | Probar en el teléfono los arreglos de cocina, editor, borradores, carpeta y plan | En el modo cocina, tocar la foto de un ingrediente no abre nada, y el atrás sale de la cocina como siempre.<br>Al editar una receta, vaciar el título y guardar avisa «Ponele un título antes de guardar.»; en un borrador, guarda con el título por defecto.<br>En Borradores, con un tag prendido que no deja nada, dice «Ninguna receta tiene estos tags.».<br>Al elegir una carpeta base que ya existe, la confirmación dice «Se va a usar X. Las categorías que falten se crean, y después se indexa lo que haya adentro.».<br>En el plan, la línea de una receta que ya no está va tachada con el borde gris, no rojo.<br>La ayuda de la ficha Fotos del editor se lee en tamaño chico.<br>Tocar *Reiniciar el plan* trae la confirmación entera a la vista, con el foco en *Cancelar*.<br>En el plan, una receta renombrada se ve con su título nuevo.<br>Abrir una receta que ya no está en Drive —borrada o en la papelera—, o su editor, dice «Esta receta ya no está en Drive.» con Volver, que sale de la receta; y deja de aparecer en las listas. | Falta probar |
| P104 | Probar en la Mac el MCP y el skill del agente | Crear el cliente *App de escritorio*, guardarlo en `~/.config/recetario/cliente.json` y correr `npm run mcp:conectar`: abre el navegador, se da el permiso y la terminal dice con qué cuenta quedó.<br>Registrar el MCP en Claude Code con `claude mcp add recetario -- npm --silent --prefix <repo> run mcp` y ver las diez herramientas.<br>Registrarlo en Claude Desktop, en `claude_desktop_config.json`, y ver las diez herramientas; si no encuentra `npm`, con la ruta de `which npm`.<br>Cargar un PDF con varias recetas: el resumen del lote antes de escribir, cada receta con su categoría y sus fotos, y al abrir la app las recetas están sin reindexar.<br>Corregir una receta que ya existe: `leer`, el cambio mostrado entero, `guardar`, y la corrección se ve en la app; una foto sumada y una sacada quedan con los números que dijo `validar`.<br>Los errores de login con su código y el paso que dice el skill: sin `cliente.json` (`sin-cliente`), sin el ítem `recetario-mcp` del Llavero (`sin-permiso`), el acceso quitado en la cuenta de Google (`permiso-revocado`), *Cancelar* en el permiso (`usuario-no-habilitado`) y sin red (`sin-red`). | Falta probar |
